# If the module being created in this example has any dependencies (resources 
# that it requires in ALL cases), then describe them here so that the example 
# can be deployed.

# example

# data "aws_vpc" "default" {
#   default = true
# }

# data "aws_subnet_ids" "default" {
#   vpc_id = data.aws_vpc.default.id
# }
